<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <data>

        <variable
            name="onClickListener"
            type="android.view.View.OnClickListener" />

        <variable
            name="todaybean"
            type="com.fr.konwledge.bean.TodayBean" />

        <variable
            name="date"
            type="java.util.Date" />
    </data>

    <RelativeLayout
        android:layout_width="match_parent"
        android:layout_height="95dp">

        <RelativeLayout
            android:padding="8dp"
            android:id="@+id/list_item"
            android:onClick="@{onClickListener}"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:background="?attr/selectableItemBackground">

            <TextView
                android:id="@+id/desc"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_alignParentStart="true"
                android:layout_toStartOf="@id/image_view"
                android:ellipsize="end"
                android:maxLines="3"
                android:text="@{todaybean.desc}"
                android:textColor="@color/text_color" />

            <TextView
                android:id="@+id/type"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_alignParentBottom="true"
                android:text="@{todaybean.type}"
                android:textColor="@color/text_color" />

            <TextView
                android:id="@+id/date"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_alignParentBottom="true"
                android:layout_marginLeft="80dp"
                android:layout_toEndOf="@id/type"
                android:text="@{todaybean.date}"
                android:textColor="@color/text_color" />

            <ImageView
                android:id="@+id/image_view"
                android:layout_width="60dp"
                android:layout_height="wrap_content"
                android:layout_alignParentEnd="true"
                android:layout_centerVertical="true"
                android:layout_margin="4dp"
                android:visibility="@{todaybean.imageViewVisibility}"
                android:src="@drawable/ic_launcher_background"
                app:imageUrl="@{todaybean.imageUrl}" />
        </RelativeLayout>

        <View
            android:layout_width="match_parent"
            android:layout_height="8dp"
            android:layout_alignParentBottom="true"
            android:background="@color/view" />
    </RelativeLayout>
</layout>